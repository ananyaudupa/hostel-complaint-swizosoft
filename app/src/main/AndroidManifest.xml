<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher_foreground"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher_foreground"
        android:supportsRtl="true"
        android:theme="@style/Theme.HostelComplaintTracker"
        tools:targetApi="31">
        
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".complaints.SubmitComplaintActivity"
            android:exported="false"
            android:label="Submit Complaint" />

        <activity
            android:name=".complaints.ComplaintHistoryActivity"
            android:exported="false"
            android:label="My Complaints" />

        <activity
            android:name=".complaints.ComplaintStatusActivity"
            android:exported="false"
            android:label="Complaint Status" />

        <activity
            android:name=".auth.LoginActivity"
            android:exported="false"
            android:label="Login" />

        <activity
            android:name=".auth.RegisterActivity"
            android:exported="false"
            android:label="Register" />

        <activity
            android:name=".warden.WardenDashboardActivity"
            android:exported="false"
            android:label="Warden Dashboard" />

        <activity
            android:name=".warden.ComplaintDetailActivity"
            android:exported="false"
            android:label="Complaint Details" />

        <activity
            android:name=".complaints.ChatActivity"
            android:exported="false"
            android:label="AI Assistant" />

        <service
            android:name=".notifications.MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

    </application>

</manifest>
